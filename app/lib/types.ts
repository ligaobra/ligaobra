export type Vaga = {
  id: string;
  titulo: string;
  descricao: string;
  cidade: string;
  estado: string;
  contato: string;
  idConstrutora: string;
};

export type Construtora = {
  id: string;
  nome: string;
  email: string;
  cidade: string;
  estado: string;
};
